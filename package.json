{
  "name": "quaranteeny",
  "description": "a lockdown-themed tamagotchi",
  "version": "1.0.0",
  "engines": {
    "node": "14.x"
  },
  "scripts": {
    "commit": "npx cz",
    "format": "cd ./client && npm run format && cd ../server && npm run format",
    "install": "cd ./client && npm i && cd ../server && npm i",
    "lint": "cd ./client && npm run lint && cd .. && cd ./server && npm run lint",
    "lint-fix": "cd ./client && npm run lint-fix && cd ../server && npm run lint-fix",
    "prepare": "husky install"
  },
  "devDependencies": {
    "commitizen": "^4.2.4",
    "cypress": "^7.2.0",
    "cz-conventional-changelog": "^3.2.0",
    "husky": "^6.0.0"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
