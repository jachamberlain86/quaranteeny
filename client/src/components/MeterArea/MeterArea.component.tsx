import React, { FC } from 'react';
import Meter from '../Meter/Meter.component';

const MeterArea: FC = () => {
  return (
    <div>
      <Meter />
      <Meter />
    </div>
  );
};

export default MeterArea;
